#include <stdio.h>

/*
 * charCount - the second version with minimun code write ups
 * optimised and simple 
 *
 * have a look
 */

int main(char c)
{
	int c;

	while ((c = getchar) != EOF)
		putchar(c);
}
