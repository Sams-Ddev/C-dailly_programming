#include <stdio.h>	/*This is a header file*/

/**
 * In the below code, we have created a function named 'func_square'
 * that takes an integer parameter 'num'. 
 * The function calculates the square of 'num'
 *
 * Returns - the result of the execution.
 */

/*We define the function (func_square) below*/
int func_square(int num)
{
	int num;
	int result;

	result = num*num;

	return (result);
}

/**/
int main(void)
{
	func_square(int num);
	return (num*num);
}
