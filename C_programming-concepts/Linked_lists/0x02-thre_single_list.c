#include <stdio.h>
#include <stdlib.h>

/*
 * This is the first vavriable of the code
 */

struct node1
{
	int data;
	struct node *link;
ii


int main()
{
	/* Creating the first node */
	struct node *head = NULL;
	struct node *head = malloc(sizeof(struct node));
	head-> data;
	head-> link;


	/* Adding the second node */
	struct node *secHead = malloc(sizeof(struct node));
	secHead-> 98;
	secHead-> NULL;
	head->link = secHead;


	/* Adding the 3rd node */
	struct node *thrdHead = malloc(sizeof(struct node));
	thrdHead-> 205;
	thrdHead-> NULL;
	sechead->link = thrdHead;



	return (0);
}

