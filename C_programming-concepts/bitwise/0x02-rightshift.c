#include <stdio.h>

/*
 * The right shift >>
 * this is a bitwise oprand that shift bits to right side
 */

int main()
{
	char var = 3;	/* note: 3 in binary is a 0000 0011 */

	printf("%d", var>>1);

	return (o);
}
