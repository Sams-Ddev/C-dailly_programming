#include <stdio.h>
#include <stdlib.h>

/**
 * findMin - computes for the middle number in an
 * array of numbers.
 */

int findMid(arr[], int n)
{
	return (&);
}

int main(void)
{
	int arr[] = {1, 2, 3, 4, 5};
	int n = sizeof(arr) / sizeof(arr[0]);

	int mid = findMid(arr, n);
	printf("%d\n". mid);

	return (EXIT_SUCCESS);
}
