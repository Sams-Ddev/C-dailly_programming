#include <stdio.h>
#include <stdlib.h>

/**
 * the simple program finds the lagest and smalllest of an
 * array of numbers.
 * @
 * @
 * @
 *
 * Return: Always 0.
 */

int main(void)
{
	/* Array elements */
	int arr[] = {20, 1, 0, 21, 11, 92, 10.5, 201.5};
	int i, max, min, arr_length;

	int arr_length = sizeof(arr) / sizeof(arr[0]);
	i = max = min = 0;
	for ()
	return (EXIT_SUCCESS);
}
